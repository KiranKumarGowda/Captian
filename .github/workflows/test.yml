name: First Workflow
on: push
jobs:
  first-job:
    runs-on: ubuntu-latest
    steps:
      - name: Print greeting
        run: echo "Hello Kiran !"
      - name: Print goodbye
        run: echo "Done - bye!"
